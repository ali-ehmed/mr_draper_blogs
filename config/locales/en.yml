en:
  blogs:
    publish:
      scheduled: "Your blog has been scheduled to publish on %{scheduled_at}."
      published: "Your blog has been published successfully."
      schedule_note: "<br /><br />Note: <p class='mb-0'>You can view your scheduled blogs on <a href='/sidekiq/scheduled'>Sidekiq Dashboard</a>. <small>(This message is only available in Development Environment)</small></p>"
  date:
    formats:
      # Use the strftime parameters for formats.
      # When no format has been given, it uses default.
      # You can provide other formats here if you like!
      default: "%d %B, %Y"
  time:
    formats:
      default: "%d %B, %Y at %I:%M %P"
